<h1><%= t('index.Edit Label Page') %></h1>
<%= render 'labels/error_messages' %>

<%= form_with(model: @labels, local: true) do |f| %>
  <div class="row gap-3">
    <div class="form-group col-12"> 
      <%= f.label :"名前" %>
      <%= f.text_field :name, class: "form-control" %>
      </div>

    <div class="form-group col-12 d-flex justify-content-end mt-3">      
      <%= f.submit "更新する", class: "btn btn-primary" %>
    </div>
  </div>
<% end %>

<%= link_to "戻る", user_path(current_user.id), id: "back", class: "d-flex justify-content-end mt-3" %>
